# Track GitHub latest release

For GitHub release sources, a tracked update policy follows GitHub's latest-release semantics rather than sorting tags locally. This keeps the first release simple and delegates prerelease and release ordering intent to the upstream project; entries that need stricter control can use a fixed release tag.
