# Use short source resolver keys

Source resolver configuration uses the short keys `url`, `github`, `aur`, and `script`. The first release implements URL, GitHub, and AUR resolvers, while `script` remains a reserved extension point so unusual upstreams can be handled later without changing the core vocabulary.
