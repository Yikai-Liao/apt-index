# Fix GitHub releases by tag

For GitHub release sources, a fixed update policy pins the release tag and then selects architecture-specific `.deb` assets from that release. Tags are the stable upstream release identity on GitHub, while Debian package versions and asset URLs are derived outputs that may use project-specific naming conventions.
